<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="service.defaultprofile"
       name="Load Default Profile Service"
       version="0.1.0"
       provider-name="LeechGuy">
  <requires>
    <import addon="xbmc.python" version="2.0"/>
  </requires>
  <extension point="xbmc.service" library="default.py" start="startup"/>
  <extension point="xbmc.addon.metadata">
    <platform>all</platform>
    <summary lang="en">Load a configurable default profile on startup of XBMC</summary>
    <description>When using multiple profiles with the login screen disabled, XBMC will automatically login the user who was logged in during the last session.

This addon allows you to override this behaviour and set a specific default profile which will always be used during login.

This addon must be configured using the Master user profile.</description>
    <license>GPL 2.0</license>
  </extension>
</addon>
